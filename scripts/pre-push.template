#!/usr/bin/env bash
#
# Pre-push hook for neo4j-api
# Runs comprehensive tests before pushing to remote
#
# This hook is called by "git push" after it has checked the remote status,
# but before anything has been pushed. If this script exits with a non-zero
# status nothing will be pushed.
#
# To bypass this hook, use: git push --no-verify

set -e  # Exit on error

# Get the git root directory
GIT_ROOT="$(git rev-parse --show-toplevel)"

# Color output
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# Source shared venv helper
source "${GIT_ROOT}/scripts/lib/venv.sh"

echo -e "${BLUE}🚀 Running pre-push checks...${NC}\n"

# Function to print section headers
print_section() {
    echo -e "\n${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
    echo -e "${BLUE}$1${NC}"
    echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}\n"
}

# Check if app/ directory exists
if [ ! -d "app" ]; then
    echo -e "${YELLOW}⚠️  Warning: app/ directory does not exist yet${NC}"
    echo -e "${YELLOW}   Skipping unit tests (no implementation to test)${NC}"
    SKIP_UNIT_TESTS=true
else
    SKIP_UNIT_TESTS=false
fi

# Check if tests/ directory exists
if [ ! -d "tests" ]; then
    echo -e "${YELLOW}⚠️  Warning: tests/ directory does not exist yet${NC}"
    echo -e "${YELLOW}   Skipping unit tests (no tests written)${NC}"
    SKIP_UNIT_TESTS=true
fi

# 1. Run unit tests with 100% coverage requirement
if [ "$SKIP_UNIT_TESTS" = false ]; then
    print_section "🧪 Running Unit Tests with 100% Coverage Requirement"

    # Activate virtual environment if needed
    if ! activate_venv_if_needed; then
        exit 1
    fi

    if ! pytest tests/ \
        --cov=app \
        --cov-report=term-missing \
        --cov-report=html:htmlcov \
        --cov-fail-under=100 \
        -v; then
        echo -e "\n${RED}❌ Unit tests failed or coverage is below 100%${NC}"
        echo -e "${RED}   Review the coverage report above${NC}"
        echo -e "${RED}   To see detailed HTML report: open htmlcov/index.html${NC}"
        echo -e "\n${YELLOW}💡 To bypass this check (not recommended): git push --no-verify${NC}"
        exit 1
    fi

    echo -e "\n${GREEN}✅ Unit tests passed with 100% coverage${NC}"
else
    echo -e "${YELLOW}⏭️  Skipping unit tests (implementation not ready)${NC}"
fi

# 2. Final summary
echo -e "\n${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${GREEN}🎉 All pre-push checks passed!${NC}"
echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}\n"
echo -e "${GREEN}✓${NC} Unit tests: ${GREEN}PASSED (100% coverage)${NC}"
echo -e "\n${GREEN}🚢 Ready to push!${NC}\n"

exit 0
