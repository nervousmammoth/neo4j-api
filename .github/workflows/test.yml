name: Test Suite

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  # Unit Tests with pytest
  unit-tests:
    name: Unit Tests (pytest)
    runs-on: ubuntu-latest
    if: ${{ hashFiles('app/**/*.py') != '' }}

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements*.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Run pytest
        run: |
          pytest tests/ \
            --cov=app \
            --cov-report=xml \
            --cov-report=html \
            --cov-report=term-missing \
            -v

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage.xml
          flags: unittests
          name: codecov-umbrella

      - name: Archive coverage reports
        uses: actions/upload-artifact@v4
        with:
          name: coverage-reports-unit
          path: htmlcov/

  # BDD Acceptance Tests with behave
  bdd-tests:
    name: BDD Acceptance Tests (behave)
    runs-on: ubuntu-latest
    needs: unit-tests
    if: ${{ always() && (needs.unit-tests.result == 'success' || needs.unit-tests.result == 'skipped') && hashFiles('app/**/*.py') != '' }}

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements*.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Run BDD tests (smoke)
        run: |
          behave features/ --tags=@smoke --format=pretty --format=json --outfile=reports/behave/smoke.json

      - name: Run all BDD tests
        run: |
          behave features/ --format=pretty --format=html --outfile=reports/behave/index.html --format=json --outfile=reports/behave/results.json

      - name: Upload BDD test reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: bdd-test-reports
          path: reports/behave/

  # Code Quality Checks
  code-quality:
    name: Code Quality
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      - name: Run Black (formatter check)
        if: ${{ hashFiles('app/**/*.py') != '' }}
        run: black --check app/ tests/ features/

      - name: Run Black (features only)
        if: ${{ hashFiles('app/**/*.py') == '' }}
        run: black --check features/

      - name: Run Ruff (linter)
        if: ${{ hashFiles('app/**/*.py') != '' }}
        run: ruff check app/ tests/ features/

      - name: Run Ruff (features only)
        if: ${{ hashFiles('app/**/*.py') == '' }}
        run: ruff check features/

      - name: Run mypy (type checking)
        if: ${{ hashFiles('app/**/*.py') != '' }}
        run: mypy app/ --strict

  # Summary Job
  test-summary:
    name: Test Summary
    runs-on: ubuntu-latest
    needs: [unit-tests, bdd-tests, code-quality]
    if: always()

    steps:
      - name: Check test results
        run: |
          echo "✅ Unit Tests: ${{ needs.unit-tests.result }}"
          echo "✅ BDD Tests: ${{ needs.bdd-tests.result }}"
          echo "✅ Code Quality: ${{ needs.code-quality.result }}"

          # Check if any job failed (skipped is OK when app/ doesn't exist)
          if [[ "${{ needs.unit-tests.result }}" == "failure" ]] || \
             [[ "${{ needs.bdd-tests.result }}" == "failure" ]] || \
             [[ "${{ needs.code-quality.result }}" == "failure" ]]; then
            echo "❌ Some tests failed"
            exit 1
          fi

          echo "🎉 All tests passed!"
